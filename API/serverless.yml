service: Booki-system-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x

functions:
  handler:
    handler: src/handlers.handler
    events:
      - http:
          path: /
          method: get

plugins:
  - serverless-plugin-typescript